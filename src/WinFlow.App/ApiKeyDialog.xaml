<Window x:Class="WinFlow.App.ApiKeyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinFlow — OpenAI API Key"
        Width="440" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Topmost="True">
    <StackPanel Margin="20">
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,12">
            WinFlow streams your speech directly to OpenAI using your own key.
            The key is stored in Windows Credential Manager, never in a file.
        </TextBlock>
        <TextBlock x:Name="StatusLabel" FontWeight="SemiBold" Margin="0,0,0,8" />
        <PasswordBox x:Name="KeyBox" Margin="0,0,0,16" Padding="6" FontFamily="Consolas" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RemoveButton" Content="Remove key" Padding="12,6" Margin="0,0,8,0" Click="OnRemove" />
            <Button Content="Cancel" Padding="12,6" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Save" Padding="12,6" IsDefault="True" Click="OnSave" />
        </StackPanel>
    </StackPanel>
</Window>
